<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create New Hisaab - Khaatabook</title>
    <link href="https://fonts.googleapis.com/css?family=Inter:400,600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Inter', Arial, sans-serif;
            background: linear-gradient(120deg, #dfe9f3 0%, #ffffff 100%);
            color: #222;
            min-height: 100vh;
        }
        nav {
            background: #fff;
            box-shadow: 0 2px 12px 0 rgba(44, 62, 80, 0.07);
            padding: 0 40px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 68px;
        }
        .nav-logo {
            color: #1769aa;
            font-size: 2rem;
            font-weight: 700;
            letter-spacing: 2px;
            display: flex;
            align-items: center;
        }
        .nav-links {
            display: flex;
            align-items: center;
            gap: 32px;
        }
        .nav-links a {
            color: #1769aa;
            text-decoration: none;
            font-size: 1.1rem;
            font-weight: 500;
            padding: 8px 18px;
            border-radius: 5px;
            transition: background 0.15s, color 0.15s;
        }
        .nav-links a:hover, .nav-links a.active {
            background: #e3f0fb;
            color: #0d3c61;
        }
        .main {
            padding: 32px 0;
            background-color: transparent;
            margin: 40px auto 0 auto;
            width: 90%;
            max-width: 600px;
            border-radius: 18px;
        }
        .heading {
            font-size: 2rem;
            font-weight: 600;
            color: #1769aa;
            margin-bottom: 30px;
            letter-spacing: 1px;
            text-align: center;
        }
        .form-container {
            background: #fff;
            padding: 38px 32px 28px 32px;
            border-radius: 18px;
            box-shadow: 0 6px 32px rgba(52, 103, 168, 0.09);
            width: 100%;
            max-width: 570px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            gap: 0;
        }
        input[type="text"], textarea {
            width: 100%;
            padding: 14px;
            margin-bottom: 20px;
            border: none;
            border-radius: 10px;
            background: #f4f8fd;
            font-size: 1.07rem;
            color: #22314a;
            resize: none;
            font-family: 'Inter', Arial, sans-serif;
            letter-spacing: 0.2px;
            transition: background 0.12s;
        }
        input[type="text"]:focus, textarea:focus {
            outline: 2px solid #1e88e5;
            background: #e3f0fb;
        }
        textarea {
            height: 160px;
        }
        #buttonn {
            background-color: #1e88e5;
            color: white;
            padding: 13px 0;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            font-size: 1.13rem;
            font-weight: 600;
            transition: background 0.18s, color 0.18s;
            box-shadow: 0 2px 8px rgba(44, 62, 80, 0.08);
            margin-top: 10px;
            width: 100%;
            letter-spacing: 1px;
        }
        #buttonn:hover {
            background-color: #14508a;
            color: #e3f0fb;
        }
        @media (max-width: 600px) {
            nav {
                flex-direction: column;
                height: auto;
                padding: 15px;
            }
            .main {
                padding: 18px 0;
            }
            .form-container {
                padding: 18px 7px 12px 7px;
            }
            .heading {
                font-size: 1.25rem;
            }
        }
    </style>
</head>

<body>
    <nav>
        <div class="nav-logo">Khaatabook</div>
        <div class="nav-links">
            <a href="/" class="active">Home</a>
            <a href="/create">Create new hisaab</a>
        </div>
    </nav>
    <div class="main">
        <div class="heading">Create New Hisaab</div>
        <form class="form-container" method="POST" action="/createhisaab" autocomplete="off">
            <input name="title" type="text" placeholder="Shopping ka hisaab, ghar ka kharch..." required />
            <textarea name="content" id="details" placeholder="Write your hisaab..." required></textarea>
            <input id="buttonn" type="submit" value="Create Hisaab">
        </form>
    </div>
</body>

</html>